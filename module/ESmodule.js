//* default export
export const add = (a, b) => {
  return a + b;
};

// export default add; => for default import

//* named export
export const sub = (p, q) => {
  return p - q;
};
//export { add, sub };
